{
  "name": "yugioh-image-processing",
  "version": "0.0.1",
  "description": "yugioh card image processing",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "babel src -d dist && node --no-warnings dist/index.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-tesseract": "^0.2.7",
    "node-webcam": "^0.4.3",
    "opencv": "^6.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-latest": "^6.24.1"
  }
}
